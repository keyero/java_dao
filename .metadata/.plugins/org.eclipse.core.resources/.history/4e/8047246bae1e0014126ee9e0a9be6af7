package day02;

import java.util.HashMap;
import java.util.Map;

public class Foo {
    private Map<Thread, Foo> map = new HashMap<Thread, Foo>();
    
    public static synchronized Foo getInstance() {
        Thread t = Thread.currentThread();
        Foo foo = map.get(t);
        if ()
    }
}
